<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="survey.survey_user_input_rule_survey_user" model="ir.rule">
            <field name="domain_force">[
                '&amp;', ('survey_id.survey_type', 'in', ('assessment', 'custom', 'live_session', 'survey', 'pos_survey')),
                '|', ('survey_id.restrict_user_ids', 'in', user.id),
                     ('survey_id.restrict_user_ids', '=', False)]</field>
        </record>
        <record id="survey.survey_user_input_line_rule_survey_manager" model="ir.rule">
            <field name="domain_force">[('survey_id.survey_type', 'in', ('assessment', 'custom', 'live_session', 'survey', 'pos_survey'))]</field>
        </record>
        <record id="survey.survey_user_input_rule_survey_manager" model="ir.rule">
            <field name="domain_force">[('survey_id.survey_type', 'in', ('assessment', 'custom', 'live_session', 'survey', 'pos_survey'))]</field>
        </record>
    </data>
</odoo>
