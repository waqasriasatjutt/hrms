<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron job to auto-close idle POS sessions -->
        <record id="ir_cron_auto_close_idle_sessions" model="ir.cron">
            <field name="name">Karage POS: Auto-close idle sessions</field>
            <field name="model_id" ref="point_of_sale.model_pos_session"/>
            <field name="state">code</field>
            <field name="code">model._cron_auto_close_idle_sessions()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">15</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
            <field name="priority">100</field>
        </record>
    </data>
</odoo>
