<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Product Brand List View -->
    <record id="product_brand_view_tree" model="ir.ui.view">
        <field name="name">product.brand.tree</field>
        <field name="model">product.brand</field>
        <field name="arch" type="xml">
            <list string="Product Brands">
                <field name="sequence" widget="handle"/>
                <field name="image_128" widget="image" options="{'size': [32, 32]}"/>
                <field name="name"/>
                <field name="code"/>
                <field name="product_count"/>
                <field name="active" invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Product Brand Form View -->
    <record id="product_brand_view_form" model="ir.ui.view">
        <field name="name">product.brand.form</field>
        <field name="model">product.brand</field>
        <field name="arch" type="xml">
            <form string="Product Brand">
                <sheet>
                    <field name="active" invisible="1"/>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="image" widget="image" class="oe_avatar" options="{'preview_image': 'image_128'}"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="Brand Name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="company_id" groups="base.group_multi_company"/>
                            <field name="product_count"/>
                        </group>
                    </group>
                    <group>
                        <field name="description" placeholder="Brand description..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Product Brand Kanban View -->
    <record id="product_brand_view_kanban" model="ir.ui.view">
        <field name="name">product.brand.kanban</field>
        <field name="model">product.brand</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <field name="name"/>
                <field name="image_128"/>
                <field name="product_count"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_image">
                                <img t-att-src="kanban_image('product.brand', 'image_128', record.id.raw_value)" 
                                     alt="Brand" class="o_image_64_cover"/>
                            </div>
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title">
                                    <field name="name"/>
                                </strong>
                                <div class="o_kanban_record_subtitle">
                                    <span><field name="product_count"/> Products</span>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Product Brand Search View -->
    <record id="product_brand_view_search" model="ir.ui.view">
        <field name="name">product.brand.search</field>
        <field name="model">product.brand</field>
        <field name="arch" type="xml">
            <search string="Search Brands">
                <field name="name"/>
                <field name="code"/>
                <filter name="filter_active" string="Active" domain="[('active', '=', True)]"/>
                <filter name="filter_archived" string="Archived" domain="[('active', '=', False)]"/>
                <separator/>
                <filter name="group_by_company" string="Company" context="{'group_by': 'company_id'}"/>
            </search>
        </field>
    </record>

    <!-- Product Brand Action -->
    <record id="product_brand_action" model="ir.actions.act_window">
        <field name="name">Product Brands</field>
        <field name="res_model">product.brand</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first product brand
            </p>
            <p>
                Product brands help you organize and filter products in Point of Sale.
            </p>
        </field>
    </record>

    <!-- Menu Items -->
    <menuitem id="menu_product_brand_root"
              name="Brands"
              parent="point_of_sale.menu_point_config_product"
              action="product_brand_action"
              sequence="5"/>

</odoo>
