<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="pos_config_form_inherit" model="ir.ui.view">
        <field name="name">pos.config.inherit</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//setting[@id='other_devices']" position="after">

                <setting id="pos_session_accessible_user"
                         string="POS Session Accessible Users"
                         help="Select the users allowed to access this POS session.">

                    <field name="pos_allowed_user_ids"
                           widget="many2many_tags"/>

                </setting>

            </xpath>

        </field>
    </record>

<!-- Override the base action to grant access to the user accessing the POS session -->
     <record id="point_of_sale.action_pos_config_kanban" model="ir.actions.act_window">
        <field name="name">Point of Sale</field>
        <field name="path">point-of-sale</field>
        <field name="res_model">pos.config</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="domain">[('pos_allowed_user_ids', 'in', uid)]</field>
        <field name="search_view_id" ref="point_of_sale.view_pos_config_search" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new PoS
            </p><p>
                Configure at least one Point of Sale.
            </p>
        </field>
    </record>


</odoo>
